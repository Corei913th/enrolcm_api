services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: enrolcm_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - enrolcm_network
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      TESSERACT_PATH: /usr/bin/tesseract
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/chromium
      BROWSERSHOT_BINARY: /usr/bin/chromium
      BROWSERSHOT_CHROME_PATH: /usr/bin/chromium

  nginx:
    image: nginx:alpine
    container_name: enrolcm_nginx
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - enrolcm_network
    depends_on:
      - app

  db:
    image: postgres:15-alpine
    container_name: enrolcm_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
    networks:
      - enrolcm_network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}" ]
      interval: 5s
      timeout: 5s
      retries: 10

  # Monitoring
  prometheus:
    image: prom/prometheus:latest
    pull_policy: never
    container_name: enrolcm_prometheus
    restart: unless-stopped
    volumes:
      - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./docker/prometheus/alert_rules.yml:/etc/prometheus/alert_rules.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
    networks:
      - enrolcm_network
    ports:
      - "9090:9090"
    depends_on:
      - alertmanager

  alertmanager:
    image: prom/alertmanager:latest
    pull_policy: never
    container_name: enrolcm_alertmanager
    restart: unless-stopped
    environment:
      - SMTP_TO=${SMTP_TO}
      - SMTP_FROM=${SMTP_FROM}
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USERNAME=${SMTP_USERNAME}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
    volumes:
      - ./docker/alertmanager/alertmanager.yml.template:/etc/alertmanager/alertmanager.yml.template
    command:
      - sh
      - -c
      - |
        envsubst < /etc/alertmanager/alertmanager.yml.template > /etc/alertmanager/alertmanager.yml
        exec /bin/alertmanager --config.file=/etc/alertmanager/alertmanager.yml
    networks:
      - enrolcm_network
    ports:
      - "9093:9093"

  grafana:
    image: grafana/grafana:latest
    pull_policy: never
    container_name: enrolcm_grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./docker/grafana/provisioning:/etc/grafana/provisioning
    networks:
      - enrolcm_network
    depends_on:
      - prometheus

  node-exporter:
    image: prom/node-exporter:latest
    pull_policy: never
    container_name: enrolcm_node_exporter
    restart: unless-stopped
    networks:
      - enrolcm_network

  cadvisor:
    image: zcube/cadvisor:latest
    pull_policy: never
    container_name: enrolcm_cadvisor
    restart: unless-stopped
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    networks:
      - enrolcm_network

  nginx-exporter:
    image: nginx/nginx-prometheus-exporter:latest
    pull_policy: never
    container_name: enrolcm_nginx_exporter
    restart: unless-stopped
    command:
      - "-nginx.scrape-uri=http://nginx:80/status"
    networks:
      - enrolcm_network
    depends_on:
      - nginx

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    pull_policy: never
    container_name: enrolcm_postgres_exporter
    restart: unless-stopped
    environment:
      DATA_SOURCE_NAME: "postgresql://${DB_USERNAME}:${DB_PASSWORD}@db:5432/${DB_DATABASE}?sslmode=disable"
    networks:
      - enrolcm_network
    depends_on:
      - db

  # Logging (ELK Stack)
  elasticsearch:
    image: elastic/elasticsearch:7.17.10
    container_name: enrolcm_elasticsearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    networks:
      - enrolcm_network
    volumes:
      - esdata:/usr/share/elasticsearch/data

  logstash:
    image: elastic/logstash:7.17.10
    container_name: enrolcm_logstash
    restart: unless-stopped
    volumes:
      - ./docker/logstash/logstash.conf:/usr/share/logstash/pipeline/logstash.conf
    networks:
      - enrolcm_network
    depends_on:
      - elasticsearch

  kibana:
    image: elastic/kibana:7.17.10
    container_name: enrolcm_kibana
    restart: unless-stopped
    ports:
      - "5601:5601"
    networks:
      - enrolcm_network
    depends_on:
      - elasticsearch

  filebeat:
    image: elastic/filebeat:7.17.10
    container_name: enrolcm_filebeat
    restart: unless-stopped
    user: root
    command: ["filebeat", "-e", "--strict.perms=false"]
    volumes:
      - ./docker/filebeat/filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - enrolcm_network
    depends_on:
      - logstash

networks:
  enrolcm_network:
    driver: bridge

volumes:
  dbdata:
    driver: local
  esdata:
    driver: local
