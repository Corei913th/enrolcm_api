{
  "name": "enrolcm-api",
  "version": "1.0.0",
  "description": "EnrolCM API - Laravel Backend",
  "private": true,
  "scripts": {
    "postinstall": "echo Puppeteer installed for PDF generation"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  }
}